import { UserRegistrationDto } from '@common/dto/user-registration.dto';

/** Contrato de criação de usuário no módulo user (campos alinhados a `UserRegistrationDto`). */
export class CreateUserDto extends UserRegistrationDto {}
